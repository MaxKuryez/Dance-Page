import React from 'react';
import ReactDOM from 'react-dom';
import Gnav from './gnav/gnav_menu';

ReactDOM.render(
  <React.StrictMode>
    <Gnav />
  </React.StrictMode>,
  document.getElementById('root')
);

